Install automake for ubuntu
---------------------------

Compile this program need aclocal and automake:

```bash
$ cd sock-0.3.2
$ sudo apt install -y automake
$ ./configuration
$ make
$ sudo make install
```
